/**
 * Neutral generation canvas.
 *
 * cdesign Generate Mode replaces this file after DESIGN_GENOME passes the
 * Novelty Gate. No demo hero or visual effect is mounted here because starter
 * composition should not leak into unrelated projects.
 */
export default function Home() {
  return <main className="min-h-[100dvh]" data-cdesign-canvas />;
}
